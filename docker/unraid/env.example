# Housarr Environment Configuration for Unraid
# Copy this file to /mnt/user/appdata/housarr/.env and edit values

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Port to access Housarr (default: 8000)
APP_PORT=8000

# Application environment (production for Unraid)
APP_ENV=production

# Debug mode (set to false in production)
APP_DEBUG=false

# REQUIRED: Generate with:
# docker run --rm php:8.2-cli php -r "echo 'base64:' . base64_encode(random_bytes(32)) . PHP_EOL;"
APP_KEY=base64:CHANGE_ME_GENERATE_A_KEY

# Your Housarr URL (update to your Unraid IP or domain)
APP_URL=http://192.168.1.100:8000

# ===========================================
# DATABASE SETTINGS
# ===========================================

# Database type: sqlite or mysql
DB_CONNECTION=mysql

# MySQL settings (if using mysql)
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=housarr
DB_USERNAME=housarr
DB_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# MySQL root password
MYSQL_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD

# ===========================================
# REDIS SETTINGS
# ===========================================

# Redis password (required for production)
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD

# ===========================================
# TIMEZONE
# ===========================================

# Your timezone (find yours: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/New_York

# ===========================================
# OPTIONAL: MAIL SETTINGS
# ===========================================

# Mail driver: smtp, mailgun, ses, sendgrid
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your@email.com
# MAIL_PASSWORD=your_app_password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=housarr@yourdomain.com
# MAIL_FROM_NAME="Housarr"

# ===========================================
# OPTIONAL: AI SETTINGS (configure in UI after install)
# ===========================================

# These can also be set in the Settings page after installation
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GOOGLE_AI_API_KEY=...
